section#projects.projects.section.v-align.centered
    .v-wrapper.text-center
        .container
            h1.title My projects
            each project in projects
                .project.sm-6.md-4
                    a(href=project.address target="_blank").name #{project.name}
                    p.description #{project.description}