.menu(class="slide-" + i)
    .wrapper
        h4.full-name
            span #{full_name}

        include ../nav/nav

        ul.social.list-unstyled
            each social, index in socials
                li
                    a(href=social.address title=index target="_blank")
                        i.fa(class="fa-" + social.icon)