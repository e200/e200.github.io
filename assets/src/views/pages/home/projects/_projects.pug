#projects.projects.section
    .container
        h1.section--title.projects--title My projects

        .row
            each project in projects
                .project-item
                    a(href=project.address target="_blank").project-item--name #{project.name}

                    p.project-item--description #{project.description}